---
name: Design Document Generator
description: Generate detailed design document based on SRS section following 3.2.1 template
category: Documentation
tags: [design, srs, documentation]
---

# 设计文档生成器 (Design Document Generator)

基于 SRS 规格文档和具体需求章节，生成详细设计文档。

## 使用方法

```
/design <章节号> <章节名称>
```

**示例**:
```
/design 3.2.2 IQC 动态路由与复判
```

---

## 工具与思考模式 (Tools & Thinking Mode)

**启用模式**: `--ultrathink` (最大深度分析，~32K tokens)

**可用工具**:
- **Sequential Thinking** (`mcp__sequential-thinking__sequentialthinking`): 用于复杂的多步骤推理、架构分析、假设验证
- **Serena Tools** (`mcp__serena__*`): 用于代码探索、符号操作、项目记忆管理
  - `find_symbol`, `search_for_pattern`, `get_symbols_overview` - 代码理解
  - `write_memory`, `read_memory`, `list_memories` - 会话持久化

**使用场景**:
- 架构决策分析 → Sequential Thinking
- 代码模式探索 → Serena search/find tools
- 复杂业务逻辑推理 → Sequential Thinking
- 跨会话上下文保持 → Serena memory tools

---

## 执行步骤

请按以下步骤生成设计文档：

### 0. 物理布局调研（针对硬件密集型功能）

**适用场景**: 涉及硬件设备、传感器、机械臂、流水线等物理设备的功能

**调研内容**:
- 询问用户物理硬件布局（流水线、工位、传感器位置）
- 了解硬件自主控制循环（如：传感器触发 → 硬件动作）
- 明确硬件职责边界（ECS 自主控制 vs ECS → WES 上报）
- 确认物理约束（如：线体容量、工位数量、尺寸限制）

**输出**: 在设计文档中创建 §3.0 物理布局与硬件配置章节

**验证**: 与用户确认物理布局描述是否准确

### 1. 读取输入文档

- 读取 `@docs/SRS.md` 中用户指定的章节
- 读取 `@docs/user_requirement.md` 中的相关需求（用于理解业务背景）
- 读取 `@docs/design/3.2.1-dock-receiving-binding.md` 作为格式参考
- 读取 `@docs/mock_data.md` 作为模拟数据参考

### 2. 生成设计文档（分阶段写入）

**重要**: 为避免 TOKEN 超长导致文件创建失败，设计文档必须分阶段生成和写入。

#### 阶段 1: 创建文档框架（Write 工具）

生成并写入文档头部和基础章节：

**包含内容**:
- 文档标题
- 技术栈说明 (Technology Stack)
  - WMS 技术架构（Blazor Server 前后端不分离）
  - WES 技术架构（Python/Java）
  - 集成架构图（Mermaid 格式）
  - Blazor 代码示例（如有 WMS-WES 交互）
  - 配置说明（appsettings.json）
  - 部署架构图（Mermaid 格式）
- 范围与依据 (Scope & Traceability)
- 角色与归属 (Actors & Ownership)
- 前提与配置 (Preconditions & Config)

**操作**: 使用 Write 工具创建文件 `docs/design/[章节号]-[功能名称].md`

#### 阶段 2: 添加核心流程设计（Edit 工具追加）

生成并追加核心业务流程：

**包含内容**:
- 子功能流程设计 (Sub-function Flows)
  - 按业务流程顺序，每个子功能包含：
    - 流程步骤（编号清晰）
    - 参与方（WMS/WES/RCS/人工/设备）
    - 数据流向
    - 校验逻辑
    - 异常处理

**操作**: 使用 Edit 工具在文件末尾追加内容

**注意**: 如果子功能流程过多（>5个），可以分批追加

#### 阶段 3: 添加时序图和对齐要点（Edit 工具追加）

生成并追加时序图和工业操作要点：

**包含内容**:
- 典型时序 (Mermaid Sequence)
  ```mermaid
  sequenceDiagram
      participant 作业员
      participant WMS
      participant SAP
      participant WES
      participant 设备
      participant RCS

      [详细时序图]
  ```
- 工业操作对齐要点 (Industrial Ops Alignment)
  - 顺序控制
  - 区域一致性
  - 安全约束
  - 幂等与恢复
  - 边界遵循
- 数据要点 (Data Notes)
  - 关键数据对象
  - 数据归属（WMS/WES）
  - 数据结构示例

**操作**: 使用 Edit 工具在文件末尾追加内容

#### 阶段 4: 添加功能页面设计（Edit 工具追加）

生成并追加界面设计：

**包含内容**:
- 功能页面设计 (Functional Page Design)
  - WMS Web 页面（每个页面包含路径、角色、页面元素、交互逻辑、关键特性）
  - WMS PDA 界面（每个界面包含界面类型、角色、界面流程、界面特性）

**操作**: 使用 Edit 工具在文件末尾追加内容

**注意**: 如果页面/界面过多（>5个），可以分批追加

#### 阶段 5: 添加特殊机制和总结（Edit 工具追加）

生成并追加特殊机制设计和文档总结：

**包含内容**:
- 特殊机制设计（如需要）
  - 双向校验机制、临时编号机制、异常处理机制、状态转换图、数据一致性保证
  - 每个机制包含：设计原则、数据库模型、业务逻辑、UI 交互设计、异常处理流程
- 总结章节（Summary）
  - 核心设计要点
  - 设计质量保证
  - 后续实施建议

**操作**: 使用 Edit 工具在文件末尾追加内容

#### 阶段 6: 添加团队分工（Edit 工具追加）

生成并追加团队分工表：

**包含内容**:
- 团队分工表 (Team Responsibilities)
  - 明确 WMS、WES、ECS 三个团队的职责边界
  - 每个功能点的实施团队
  - 跨团队协作接口

**表格格式**:
| 功能点 | 功能简述 | 团队分工 | 备注 |
|--------|----------|----------|------|
| [功能名称] | [简要描述] | WMS/WES/ECS/协同 | [特殊说明] |

**团队说明**:
- **WMS 团队**: ASP.NET (Blazor Server) - 负责业务逻辑、Web 界面、PDA 界面、SAP 集成
- **WES 团队**: Python (FastAPI) - 负责调度引擎、策略执行、硬件协调、状态管理
- **ECS 团队**: 硬件开发 - 负责设备控制、机器人臂、视觉系统、传感器集成
- **协同**: 需要多团队配合完成的功能

**操作**: 使用 Edit 工具在文件末尾追加内容

#### 文件写入策略

1. **阶段 1 使用 Write**: 创建新文件，写入文档框架
2. **阶段 2-6 使用 Edit 追加**: 在文件末尾追加内容
3. **每个阶段独立完成**: 确认上一阶段写入成功后再进行下一阶段
4. **灵活分批**: 如果某个阶段内容过多，可以进一步细分为多次追加
5. **验证写入**: 每次写入后可以读取文件末尾部分验证是否成功

### 3. 架构边界原则（关键！）

**在设计过程中，必须遵循以下原则**:

1. **职责明确**:
   - 明确每个功能由 WMS 还是 WES 负责
   - 不要假设 WES 参与所有自动化流程
   - 当前阶段可能 WMS 独立完成某些功能

2. **API 设计**:
   - 只设计真正需要的 WMS-WES 集成 API
   - 不要设计 WMS 内部 API
   - 不要设计 WES 南向 API（设备接口）
   - 不要设计 WES 内部 API

3. **设备管理**:
   - 明确打印机由谁管理（WMS 直接 TCP？还是通过 WES？）
   - 明确 RCS 由谁调度（WMS 直接？还是通过 WES？）
   - 明确 PDA 连接到哪个系统

4. **数据归属**:
   - 明确哪些数据由 WMS 持有
   - 明确哪些数据由 WES 持有
   - 避免数据重复维护

5. **简化优先**:
   - 使用第一性原理思考
   - 避免过度设计
   - 能自动化的就自动化
   - 能简化的就简化

### 4. 输出格式

**文件名**: `docs/design/[章节号]-[功能名称].md`

例如: `docs/design/3.2.2-iqc-routing-review.md`

**语言**: 中文为主，技术术语保留英文，格式为"中文 (English)"

**Markdown 格式**:
- 使用标准 Markdown 语法
- 代码块使用 ```语言标识
- 表格使用标准 Markdown 表格
- Mermaid 图使用 ```mermaid

### 5. 迭代改进机制

如果用户指出架构边界错误（如"WES 不参与此阶段"），应该：
1. 立即更新相关章节
2. 移除不必要的 API 设计
3. 更新角色与归属表
4. 更新时序图
5. 更新代码示例
6. 确保文档内部一致性

**不要争辩，直接按用户指示修正**。

### 6. 设计审计

设计文档完成后，使用 `/audit-design` 命令进行审计：
```
/audit-design docs/design/[章节号]-[功能名称].md
```

审计将检查：
- 与 user_requirement.md 的需求覆盖率
- 内部一致性（角色、流程、时序、界面、数据）
- 架构合规性
- 可实施性

---

## 关键经验教训（从 3.2.1 设计中学到的）

### 架构边界教训

1. **不要假设 WES 参与所有流程**
   - 3.2.1 最终确认：WMS 全权负责码头收货绑定，WES 不参与
   - 教训：先明确职责边界，再设计流程

2. **不要假设需要 WMS-WES API**
   - 3.2.1 最终确认：无需任何 WMS-WES API
   - 教训：只设计真正需要的集成接口

3. **不要假设 WES 管理打印机**
   - 3.2.1 最终确认：WMS 通过 TCP 端口 9100 直接管理打印机
   - 教训：明确设备管理归属

4. **不要假设 WES 调度 RCS**
   - 3.2.1 最终确认：WMS 直接调度 RCS
   - 教训：明确硬件调度归属

### 业务逻辑教训

1. **箱数 ≠ 栈板数**
   - 不能从箱数自动推导栈板数
   - 需要人工根据实际情况确定

2. **一个箱子不可能需要多个栈板**
   - 但一个栈板可以装多个箱子

3. **使用第一性原理简化设计**
   - 去除不必要的确认页面
   - 自动化能自动化的步骤
   - 后台执行不阻塞用户

### 设计流程教训

1. **先读 SRS，再读 user_requirement**
2. **先明确架构边界，再设计流程**
3. **先设计核心流程，再设计 UI**
4. **先设计正常流程，再设计异常处理**
5. **持续审计，确保一致性**

---

## 关键经验教训（从 3.2.2 设计中学到的）

### 工作流模式教训

1. **不要假设自动推送 (Push) 总是最优**
   - 3.2.2 最初设计：自动触发送检，可能导致 IQC 待检区拥堵
   - 3.2.2 最终确认：采用拉动式 (Pull) 模式，IQC 人员根据工作负荷主动呼叫
   - 教训：考虑现场容量限制和人员控制需求，拉动式可能比推动式更有效

2. **不要假设自动完成总是可靠**
   - 3.2.2 最初设计：检验完成后自动触发路由
   - 3.2.2 最终确认：需要人工确认触发，确保样品归还和结果录入
   - 教训：关键节点需要人工确认，但要配合兜底机制（定时轮询）防止遗忘

3. **提醒机制 + 兜底机制 = 可靠性**
   - 高优先级提醒：优先级 ≥ 8 且等待 > 1小时
   - 长时间等待提醒：等待 > 4小时
   - 兜底机制：30分钟定时扫描，自动完成被遗忘的任务
   - 教训：设计要考虑人的遗忘，用技术手段保证流程不中断

### 业务实体关系教训

1. **一个栈板可能包含多个 GRN**
   - 3.2.2 最初设计：假设一个栈板对应一个 GRN
   - 3.2.2 实际情况：一个栈板可能有多个不同 GRN，处于不同检验状态
   - 教训：不要假设一对一关系，要考虑一对多、多对多的复杂关系

2. **一个 GRN 可能分布在多个栈板上**
   - 3.2.2 关键场景：GRN 检验 NG 时，需要移动所有包含该 GRN 的栈板
   - 3.2.2 设计方案：批量查询 + 批量生成搬运任务
   - 教训：业务实体的物理分布可能很复杂，要考虑批量操作

3. **管理对象选择：业务逻辑 > 物理操作**
   - 3.2.2 关键决策：以 GRN 还是栈板为主管理对象？
   - 3.2.2 最终确认：以 GRN 为主（QMS 策略、检验结果、优先级都针对 GRN）
   - 教训：选择管理对象时，优先考虑业务逻辑而非物理操作便利性

### 状态管理教训

1. **多状态实体需要状态筛选**
   - 3.2.2 场景：栈板上有多个 GRN，状态包括：未检、检验中、已完成、免检
   - 3.2.2 设计方案：只处理"检验中"状态的 GRN，其他状态不影响当前操作
   - 教训：处理多状态实体时，要明确筛选条件，避免误操作

2. **状态转换要考虑前置条件**
   - 3.2.2 场景：栈板上还有未检的 GRN 时，不能移动栈板
   - 3.2.2 设计方案：检查所有 GRN 状态，只有全部完成才移动栈板
   - 教训：状态转换前要检查所有相关实体的状态，不能只看当前操作的实体

3. **状态可视化很重要**
   - 3.2.2 设计增强：界面清晰区分"检验中 GRN"和"其他 GRN"
   - 3.2.2 设计增强：实时显示样品归还状态和 QMS 结果状态
   - 教训：复杂状态要可视化，让用户清楚知道当前状态和可执行操作

### 触发机制教训

1. **明确触发方式：自动 vs 人工**
   - 3.2.2 最初设计：触发方式不明确
   - 3.2.2 最终确认：送检呼叫（人工）、检验完成（人工+兜底）、复判完成（人工+兜底）
   - 教训：每个流程节点都要明确触发方式，不能含糊

2. **容量监控要实时可见**
   - 3.2.2 设计增强：实时显示 IQC 待检区容量（如：3/10）
   - 3.2.2 设计增强：选择栈板时动态显示容量变化
   - 教训：容量限制要可视化，帮助用户做决策

### 设计演进教训

1. **接受用户挑战，不要固守初始设计**
   - 用户质疑："自动触发有很大问题，是否由 IQC 人员自主选择会更有效？"
   - 设计响应：立即改为拉动式模式，重新设计整个流程
   - 教训：用户的现场经验往往比设计者的假设更准确

2. **通过提问发现隐藏需求**
   - 用户提问："如果该栈板中有多个不同的 GRN，该如何操作？"
   - 设计响应：发现多 GRN 栈板场景，重新设计状态管理
   - 教训：用户的"如果"问题往往揭示真实的复杂场景

3. **架构决策要基于业务本质**
   - 用户提问："IQC 待送检列表中应该是以栈板还是以 GRN 为主管理对象？"
   - 设计分析：QMS 策略针对 GRN、检验结果针对 GRN、优先级针对 GRN
   - 教训：架构决策要回归业务本质，不要被物理操作束缚

4. **设计要支持灵活性**
   - 3.2.2 设计方案：GRN 为主管理对象，但允许选择具体栈板
   - 3.2.2 设计方案：一个 GRN 可以分批送检（选择不同栈板）
   - 教训：设计要在业务逻辑和操作灵活性之间找到平衡

### 界面设计教训

1. **两步选择比一步选择更灵活**
   - 3.2.2 设计方案：先选择 GRN，再选择该 GRN 分布的栈板
   - 好处：用户可以根据容量、位置、数量灵活选择
   - 教训：不要过度简化界面，适当的步骤分解能提供更好的控制

2. **确认对话框要显示影响**
   - 3.2.2 设计增强：确认对话框显示"当前容量 3/10 → 5/10"
   - 3.2.2 设计增强：显示预计到达时间、优先级等关键信息
   - 教训：确认对话框不只是确认，要让用户看到操作的影响

3. **实时更新 > 手动刷新**
   - 3.2.2 技术选择：使用 SignalR 实现实时状态更新
   - 好处：样品归还、QMS 结果录入后，界面自动更新
   - 教训：关键状态变化要实时推送，不要让用户手动刷新

### 数据一致性教训

1. **批量操作要保证原子性**
   - 3.2.2 场景：一个 GRN NG 时，需要移动多个栈板
   - 3.2.2 设计考虑：批量生成搬运任务，要么全部成功，要么全部失败
   - 教训：批量操作要考虑事务性，避免部分成功部分失败

2. **跨系统数据要明确归属**
   - 3.2.2 明确：WMS 持有栈板、GRN、库存数据
   - 3.2.2 明确：QMS 持有抽样策略、检验结果数据
   - 教训：不要让两个系统都维护同一份数据，明确数据归属

### 设计流程教训（补充 3.2.1 的教训）

1. **先理解业务实体关系，再设计流程**
2. **先明确触发机制，再设计自动化**
3. **先设计状态管理，再设计状态转换**
4. **先考虑异常场景，再优化正常流程**
5. **持续接受用户反馈，迭代改进设计**

---

## 关键经验教训（从 3.3.1 设计中学到的）

### 物理布局优先原则

1. **先调研物理布局，再设计功能流程**
   - 3.3.1 最初设计：未明确双线体系统和串杆机构，导致设计不完整
   - 3.3.1 最终确认：Line 1 (7寸专用) + Line 2 (13/15寸优先)，8工位串杆步进系统
   - 教训：物理硬件约束决定了业务流程的可行性，必须优先调研

2. **物理布局应作为 §3.0 独立章节**
   - 3.3.1 设计方案：新增 §3.0 物理布局与硬件配置
   - 包含内容：双线体系统、串杆机构、视觉系统、机械臂系统
   - 教训：物理布局是后续功能流程的基础，应该最先设计和验证

3. **物理约束驱动路由逻辑**
   - 3.3.1 场景：7寸料盘优先 Line 1，13/15寸料盘强制 Line 2
   - 3.3.1 设计方案：在 §3.1 装箱任务呼叫中增加线体路由逻辑
   - 教训：物理约束不是实现细节，而是业务逻辑的一部分

### 业务实体 vs 物理实体

1. **区分业务实体和物理实体**
   - 3.3.1 最初设计：界面显示"选择 GRN 呼叫装箱任务"
   - 3.3.1 实际情况：RCS 搬运的是栈板 (Pallet)，拆箱操作的是栈板
   - 3.3.1 最终确认：改为"选择栈板 (Pallet) 呼叫装箱任务"
   - 教训：业务实体 (GRN) 和物理实体 (Pallet) 是不同的，操作要匹配物理实体

2. **UI 应反映实际操作对象**
   - 3.3.1 错误示例：作业员选择 GRN → RCS 搬运栈板（实体不匹配）
   - 3.3.1 正确示例：作业员选择栈板 → RCS 搬运栈板（实体匹配）
   - 教训：UI 选择的对象应该与后续物理操作的对象一致

3. **业务实体可以关联到物理实体**
   - 3.3.1 设计方案：选择栈板后，系统显示该栈板关联的 GRN 信息
   - 好处：既满足物理操作需求，又提供业务上下文
   - 教训：不要混淆两种实体，而是建立清晰的关联关系

### 硬件自主控制边界

1. **ECS 应自主处理本地控制循环**
   - 3.3.1 最初设计：未明确谁触发视觉扫描
   - 3.3.1 最终确认：串杆步进 → 传感器检测 → ECS 自动触发视觉扫描
   - 教训：硬件的本地控制循环应由 ECS 自主完成，无需 WES 微管理

2. **仅上报业务相关的最终结果**
   - 3.3.1 最初设计：可能需要上报"料盘到达工作位"等中间状态
   - 3.3.1 最终确认：ECS 仅上报视觉扫描结果，不上报中间状态
   - 教训：减少不必要的网络交互，WES 只关心业务相关的结果

3. **移除不必要的 WES → ECS 接口**
   - 3.3.1 最初设计：可能包含 `/ecs/api/vision/scan-pkg` 接口
   - 3.3.1 最终确认：移除该接口，ECS 自主触发扫描
   - 教训：如果 ECS 能自主完成，就不需要 WES 调用接口

### 系统职责边界澄清

1. **物理控制 vs 协调上报**
   - 3.3.1 关键问题：串杆管理是否需要 WES/WMS 交互？
   - 3.3.1 明确边界：
     - 串杆步进控制、料盘检测、视觉触发 = ECS 职责（无需上报）
     - 视觉扫描结果上报 = ECS → WES（协调所需）
   - 教训：区分纯硬件控制和需要协调的状态上报

2. **不要假设 WES 需要知道所有硬件状态**
   - 3.3.1 设计原则：WES 只需要知道业务决策所需的状态
   - 3.3.1 示例：WES 不需要知道串杆在哪个工位，只需要知道扫描结果
   - 教训：过度上报会增加系统耦合度和网络负担

3. **硬件职责边界要在设计文档中明确**
   - 3.3.1 设计方案：在 §3.0 中增加"ECS 自主控制"和"硬件职责边界"小节
   - 好处：让开发团队清楚知道哪些是 ECS 内部逻辑，哪些需要跨系统交互
   - 教训：职责边界不清晰会导致接口设计混乱

### 迭代验证的重要性

1. **初始设计假设可能不正确**
   - 3.3.1 场景：设计时未了解双线体系统和串杆机构
   - 3.3.1 结果：用户审查时发现设计缺少关键物理逻辑
   - 教训：不要假设自己完全理解业务，要主动验证

2. **用户反馈揭示物理现实**
   - 3.3.1 用户反馈：
     - "是否应该是按栈板来呼叫，而不是选择 GRN 呼叫？"
     - "这个物理逻辑是否能通过验证？"（提供双线体和串杆详细信息）
     - "串杆管理应该是 ECS 的任务吧？"
   - 教训：用户的每个问题都可能揭示设计中的假设错误

3. **设计要支持快速迭代**
   - 3.3.1 设计过程：
     - 初始设计 → 用户审查 → 发现问题 → 快速修正 → 再次验证
   - 3.3.1 修正内容：
     - 新增 §3.0 物理布局
     - 修正 §3.1 为栈板呼叫
     - 更新 §3.4 为 ECS 自主触发
     - 更新时序图和团队分工表
   - 教训：设计文档要易于修改，支持快速迭代

4. **明确物理逻辑以便验证**
   - 3.3.1 设计改进：详细描述双线体、串杆、工作位、交互流程
   - 好处：用户可以根据详细描述验证设计是否符合物理现实
   - 教训：物理逻辑要写得足够详细，让领域专家能够验证

### 时序图设计教训

1. **时序图要展示所有操作模式**
   - 3.3.1 最初设计：只展示人工作业模式
   - 3.3.1 用户反馈："完整装箱流程时序图中并没有标明自动调度栈板的流程？"
   - 3.3.1 最终确认：使用 `alt/else` 展示人工模式和系统模式
   - 教训：时序图要覆盖所有业务场景，不能只展示主流程

2. **时序图要反映硬件自主性**
   - 3.3.1 设计改进：
     ```
     ECS->>ECS: 串杆步进到工作位 (Position 4)
     ECS->>ECS: 传感器检测料盘到达
     ECS->>ECS: 自动触发视觉扫描（无需 WES 介入）
     ECS->>WES: 上报扫描结果（仅最终结果）
     ```
   - 教训：时序图要清晰展示哪些是 ECS 内部循环，哪些是跨系统交互

### 设计流程教训（补充 3.2.1、3.2.2 的教训）

1. **先调研物理布局，再设计功能流程**
2. **先区分业务实体和物理实体，再设计操作流程**
3. **先明确硬件自主控制边界，再设计系统交互**
4. **先验证物理逻辑，再完善业务逻辑**
5. **持续接受用户反馈，快速迭代设计**

---

## 关键经验教训（从 3.3.1 设计中学到的 - 第二轮修正）

### 外部团队协作原则

1. **不要描述外部团队的实现细节，只定义需求和接口**
   - 3.3.1 最初设计：详细描述 ECS 串杆步进控制逻辑、传感器检测流程、倒计时管理
   - 3.3.1 用户反馈：\"ECS 是外部团队，我们不关注其的任务详情，只需要给其提出需求即可！\"
   - 3.3.1 最终确认：改为能力需求、接口规范、性能要求、职责边界
   - 教训：外部团队的设计文档应聚焦 WHAT（需要什么能力），而非 HOW（如何实现）

2. **区分内部控制循环和跨系统交互**
   - 3.3.1 最初设计：可能包含 WES 触发视觉扫描的接口
   - 3.3.1 最终确认：ECS 自主触发扫描，仅上报扫描结果
   - 教训：硬件的本地控制循环应由硬件团队自主完成，只上报业务相关的最终结果

3. **接口设计要聚焦协调所需，不要微管理**
   - 修正前示例：
     ```
     POST /ecs/api/conveyor/step-forward  # 微管理：每步都要 WES 调用
     POST /ecs/api/vision/scan-pkg        # 微管理：WES 手动触发扫描
     ```
   - 修正后示例：
     ```
     POST /ecs/api/conveyor/start-interval-loading  # 宏观控制：启动模式
     POST /api/v1/callback/result                   # 结果上报：ECS 主动上报
     ```
   - 教训：接口应该是高层次的协调指令，不是低层次的控制命令

### 数据可用性验证原则

1. **不要假设数据存在，要验证数据来源**
   - 3.3.1 最初设计：假设 WMS 能获取 `箱内料盘总数`，并据此计算 `rods_needed`
   - 3.3.1 用户反馈：\"WMS 无需计算上料参数，也没有数据能支撑获取到 `箱内料盘总数`\"
   - 3.3.1 最终确认：移除 WMS 计算逻辑，改为 ECS 自主管理步进节奏
   - 教训：设计计算逻辑前，先验证所需数据是否在该时间点可用

2. **数据可用性验证清单**
   - 问题 1：这个数据在哪个系统？（WMS/WES/SAP/QMS）
   - 问题 2：这个数据在什么时候可用？（收货时/扫描后/检验后）
   - 问题 3：这个数据的来源是什么？（人工录入/系统计算/外部接口）
   - 问题 4：如果数据不可用，有替代方案吗？

3. **避免"便利性假设"**
   - 错误思维：\"如果 WMS 知道料盘总数，就能提前计算需要多少串杆，这样更高效\"
   - 正确思维：\"WMS 实际上没有料盘总数数据，所以不能做这个计算，需要改用其他方案\"
   - 教训：不要因为某个设计"看起来更好"就假设数据存在，要基于数据现实设计

### 物理约束验证原则

1. **不要假设物理约束，要验证实际限制**
   - 3.3.1 最初设计：假设 `每根串杆最多 7 个料盘` 的限制
   - 3.3.1 用户反馈：\"没有 `每根串杆最多 7 个料盘`的限制\"
   - 3.3.1 最终确认：移除该限制，ECS 自主管理容量
   - 教训：物理约束要基于实际硬件规格，不要基于"常见模式"假设

2. **物理约束验证清单**
   - 问题 1：这个约束来自哪里？（硬件规格/安全要求/操作规范）
   - 问题 2：这个约束是硬性的还是软性的？（物理限制 vs 建议值）
   - 问题 3：这个约束是否已在硬件层面强制？（ECS 自动控制 vs 需要 WMS 控制）
   - 问题 4：如果约束不存在，设计会如何变化？

3. **区分硬件约束和业务规则**
   - 硬件约束示例：串杆物理长度限制、机械臂工作范围、传送带承重
   - 业务规则示例：每批次最多处理 N 个料盘、优先级排序规则
   - 教训：硬件约束由硬件团队管理，业务规则由业务系统管理，不要混淆

### 任务模型设计原则

1. **选择正确的任务粒度**
   - 3.3.1 最初设计：基于料盘数量计算任务（tray-level calculation）
   - 3.3.1 用户反馈：\"每个栈板就是一个任务\"
   - 3.3.1 最终确认：改为栈板级任务模型（pallet-level task）
   - 教训：任务粒度应该基于业务自然单元，不是技术便利性

2. **任务粒度选择清单**
   - 问题 1：什么是业务上的自然工作单元？（栈板/箱/料盘/批次）
   - 问题 2：什么必须作为原子操作完成？（不可中断的最小单元）
   - 问题 3：任务状态如何跟踪？（粒度越细，状态管理越复杂）
   - 问题 4：任务失败如何恢复？（粒度影响恢复策略）

3. **避免过度优化的任务模型**
   - 错误思维：\"如果提前计算需要多少串杆，可以优化资源分配\"
   - 正确思维：\"栈板是自然工作单元，让硬件自主管理资源更简单可靠\"
   - 教训：简单的任务模型通常比复杂的优化模型更可靠、更易维护

### 任务完整性原则

1. **定义清晰的任务边界**
   - 3.3.1 用户反馈：\"必须对栈板内的任务清理完成后（包含处理好所有的 NG 料盘）才可以开始下一个栈板的任务\"
   - 3.3.1 最终确认：栈板任务包括所有正常料盘和 NG 料盘的处理
   - 教训：任务边界要包含所有相关工作，包括异常处理

2. **任务完整性检查清单**
   - 问题 1：任务的前置条件是什么？（开始前必须满足的条件）
   - 问题 2：任务的完成标准是什么？（什么情况下算完成）
   - 问题 3：任务的异常情况如何处理？（NG 料盘、设备故障等）
   - 问题 4：任务之间的依赖关系是什么？（必须完成 A 才能开始 B）

3. **避免"乐观路径"设计**
   - 错误思维：\"正常情况下，扫描完就可以开始下一个栈板了\"
   - 正确思维：\"必须处理完所有料盘（包括 NG）才能开始下一个栈板\"
   - 教训：任务完整性设计要考虑异常路径，不能只考虑正常路径

### 迭代设计流程教训

1. **接受多轮修正是正常的**
   - 3.3.1 设计经历：初稿 → 物理布局修正 → 外部团队边界修正 → 业务逻辑修正
   - 教训：复杂设计需要多轮迭代，不要期望一次就完美

2. **每轮修正聚焦不同层面**
   - 第一轮：物理布局、硬件配置、实体关系
   - 第二轮：团队边界、接口设计、职责划分
   - 第三轮：业务逻辑、数据流、任务模型
   - 教训：分层验证比一次性验证更有效

3. **用户反馈是设计改进的关键**
   - 3.3.1 关键反馈：
     - \"ECS 是外部团队，我们不关注其的任务详情\"
     - \"WMS 无需计算上料参数，也没有数据能支撑\"
     - \"每个栈板就是一个任务\"
   - 教训：每个用户反馈都可能揭示设计中的根本性问题，要认真对待

### 设计流程教训（补充 3.2.1、3.2.2、3.3.1 第一轮的教训）

1. **先明确团队性质（内部/外部），再选择设计方法**
2. **先验证数据可用性，再设计计算逻辑**
3. **先验证物理约束，再编码业务规则**
4. **先确定任务粒度，再设计任务流程**
5. **先定义任务完整性，再设计状态转换**
6. **接受多轮迭代，每轮聚焦不同层面**

---

## 执行清单

使用 TodoWrite 跟踪以下任务：

**准备阶段**:
- [ ] 读取 SRS.md 指定章节
- [ ] 读取 user_requirement.md 相关内容
- [ ] 读取参考设计文档（3.2.1）
- [ ] 读取 mock_data.md 模拟数据

**文档生成阶段**（分阶段写入，避免 TOKEN 超长）:
- [ ] 阶段 1: 创建文档框架（Write 工具）
- [ ] 阶段 2: 添加核心流程设计（Edit 追加）
- [ ] 阶段 3: 添加时序图和对齐要点（Edit 追加）
- [ ] 阶段 4: 添加功能页面设计（Edit 追加）
- [ ] 阶段 5: 添加特殊机制和总结（Edit 追加）
- [ ] 阶段 6: 添加团队分工（Edit 追加）

**验证阶段**:
- [ ] 验证文档完整性（读取文件检查）
- [ ] 使用 /audit-design 命令进行审计
